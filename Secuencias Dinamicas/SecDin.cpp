#include <iostream>
#include "SecDin.h"
#include <array>
#include <assert.h>

int main(){
	unsigned longitud (const SecDin& s);
	{
		return s.n;
	}
int get (const SecDin& s, unsigned);
{
	return s.a.at(i);
}
}
